# My Project
Git Installtion 
--------------------------
Git is a version control system used to track changes in code and collaborate with others.
25.01
25.02
25.03
-------------------------

Step 1: Download and Install Git
Visit: https://git-scm.com/download/win

The download will start automatically.

Run the installer.

🔧 During installation, keep default options, unless you know what you're changing. Especially:

✅ Use Git from the command line and also from 3rd-party software

✅ Checkout Windows-style, commit Unix-style line endings

✅ Use MinTTY (default terminal)


# Start a project
git --version
git config --global user.name "sureshreddy666"
git config --global user.email "pnpsureshreddy.com"

# Connect to GitHub
git remote add origin git@github.com:username/repo.git
git push -u origin main


Developer -> branhc 
front developer -> branch 
sql developer -> branhc 
testing branch -> branch 